### Fix #5 — SW cache js/config.js (config lama stuck)
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-11 | `sw.js:7` | `js/config.js` di-cache service worker — perubahan config tidak生效 tanpa clear cache manual | `ASSETS_TO_CACHE` mencantumkan `./js/config.js` | Hapus baris `./js/config.js` dari `ASSETS_TO_CACHE` (dikomentari), beri komentar penjelasan | SW tetap valid (cache hanya aset statis) ✓ | File konfigurasi dinamis TIDAK boleh di-cache service worker — selalu gunakan network-first untuk file yang bisa berubah | `service-worker` `cache` `config.js` | PENDING verifikasi |

### Fix #3 — anti-freeze.sql vs schema.sql inkonsisten (kolom is_frozen/last_active tidak ada)
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-11 | `supabase/schema.sql` | Tabel `announcements`, `blood_requests`, `donor_events` tidak punya kolom `is_frozen` dan `last_active` yang dibutuhkan `anti-freeze.sql` dan `freeze.js` | Skema lama belum di-update, `anti-freeze.sql` dibuat terpisah | Tambahkan kolom `is_frozen BOOLEAN DEFAULT FALSE` dan `last_active timestamptz` ke ketiga tabel di `schema.sql`, tambahkan `UPDATE ... SET last_active = created_at WHERE last_active IS NULL` untuk setiap tabel | Schema.sql syntax OK ✓; Anti-freeze.sql sekarang hanya melakukan ALTER COLUMN IF NOT EXISTS (aman dijalankan berulang) | Tambahkan kolom migration di schema.sql utama, anti-freeze.sql tetap sebagai fallback aman | `schema.sql` `is_frozen` `last_active` `anti-freeze` | PENDING verifikasi |

### Fix #4 — freeze.js akses kolom tidak ada (cron job silent fail)
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-11 | `api/freeze.js` | Query `.update(...).select('count')` tidak valid di Supabase JS — selalu return error yang di-catch silent | Pola query salah: `.update()` tidak support `.select('count')` | Rewrite: gunakan `.select('*', { count: 'exact', head: true })` untuk hitung, lalu `.update()` terpisah dua kali (null check + date check) | `node -c api/freeze.js` ✓; Query logic lebih jelas: count dulu, baru update | Jangan campur `.update()` dengan `.select('count')` di Supabase — gunakan query terpisah | `freeze.js` `count` `update` `cron` | PENDING verifikasi |

### Fix #6 — Password demo ≠ password schema (minor, opsional)
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-11 | `app.js:49` | Password demo `admin123` berbeda dengan schema.sql `<sandi akun utama — tidak ditulis di repo>` — membingungkan developer | Demo mode & prod schema dibuat terpisah tanpa sinkronisasi | Tidak diubah — demo dan prod memang terpisah. Demo tetap `admin123`, schema tetap memakai sandi rahasia dari `v_password` | — | Mode demo dan production menggunakan kredensial terpisah — jangan dibuat sama | `demo` `password` `admin123` | SKIP (intentional) |

### Fix #7 — Hapus .env dari git history + servis role key perlu di-regenerate
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-11 | `.env` (history) | Service role key terekspos di commit `d76491a` yang sudah pernah di-push ke remote | `.env` berisi secret masuk repo meski sudah di `.gitignore` — tapi history lama masih menyimpannya | `git filter-branch` hapus `.env` dari semua commit, hapus `refs/original/` backup refs, `git reflog expire`, `git gc --prune=now --aggressive` | `git log --all -- .env` kosong ✓; `git cat-file -t d76491a` → `fatal: could not get object info` ✓; hanya `.env.example` tersisa ✓ | `.gitignore` saja tidak cukup jika file sudah pernah di-commit — harus rewrite history + force push | `git-filter-branch` `.env` `service_role_key` `regenerate` | ⚠️ SERVICE ROLE KEY WAJIB DI-REGENERATE di Supabase Dashboard sebelum force push |

### Fix #1 — handleAddEvent end_time = start_time (salah ambil nilai input)
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-11 | `app.js:928` | `end_time` sama dengan `start_time` karena keduanya mengambil dari `$('#inputEventTime').value.trim()` yang sama | Field input jam donor cuma satu, tapi kode menyalin nilai yang sama ke dua kolom | Parse input dengan regex `^([^-]+)\s*[-–—]\s*(.+)$` untuk mengambil bagian sebelum dan sesudah pemisah (`-`/`–`/`—`) sebagai `start_time` dan `end_time` terpisah | Syntax check `node -c app.js` ✓; test parsing: `"09.00 - 13.00 WITA"` → end_time `"13.00 WITA"` ✓; input kosong/null tetap `null` ✓ | Selalu periksa apakah satu input field dipakai untuk dua field berbeda — jangan copy paste value tanpa parsing | `handleAddEvent` `end_time` `start_time` `inputEventTime` | PENDING verifikasi |

### Fix #8 — Login super admin gagal: "Database error querying schema"
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-12 | `supabase/schema.sql:150-190`, RPC `create_admin_user()` | Login `cecemeri48@gmail.com` selalu gagal dengan toast "Database error querying schema" (HTTP 500 dari GoTrue), bukan "kata sandi salah" | INSERT langsung ke `auth.users` tidak mengisi 8 kolom token (`confirmation_token`, `recovery_token`, `email_change`, `email_change_token_new`, `email_change_token_current`, `phone_change`, `phone_change_token`, `reauthentication_token`) sehingga bernilai NULL. GoTrue memindai kolom itu sebagai Go `TEXT` non-null → `converting NULL to string is unsupported`. Risiko sekunder: `public.current_role()` menimpa kata kunci cadangan PostgreSQL | Jalankan `fix-login-superadmin.sql`: UPDATE `auth.users` set 8 kolom token ke `''`, pastikan baris `auth.identities`, reset sandi, pastikan `user_roles.role='superadmin'`, rename `current_role()` → `peran_saya()` + recreate `is_admin`/`list_admins`/`delete_admin_user`/`create_admin_user`. `schema.sql` ikut dipatch agar kedua INSERT mengisi 8 kolom itu | Login super admin berhasil ✓ | Jangan pernah INSERT manual ke `auth.users` tanpa mengisi SEMUA kolom token — GoTrue tidak toleran NULL. Error 500 "querying schema" = masalah struktur data, bukan kredensial | `auth.users` `confirmation_token` `NULL` `querying schema` `GoTrue` | ✅ SELESAI |

### Fix #9 — Simpan stok ditolak: "new row violates row-level security policy"
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-12 | `supabase/schema.sql` §6, `app.js:553`, `app.js:584` | Petugas sudah login tapi tombol Simpan menghasilkan `new row violates row-level security policy for table "blood_stock"` | `app.js` memakai `.upsert(payload, { onConflict: 'golongan' })` yang di Postgres menjadi `INSERT ... ON CONFLICT`, sehingga WAJIB lolos policy INSERT. Schema hanya mendefinisikan policy SELECT dan UPDATE. Bug laten yang sama ada di `settings` (`app.js:584`) | Jalankan `fix-rls-simpan-stok.sql`: tambah policy `"petugas insert stok"` dan `"petugas insert settings"`, recreate policy UPDATE, dan buat `is_admin()` mandiri (membaca `user_roles` langsung, tidak bergantung pada `current_role()`/`peran_saya()`). `schema.sql` ikut dipatch | Simpan stok berhasil ✓; audit seluruh `sb.from(...)` di `app.js` — tabel lain sudah tertutup policy-nya | `upsert` = INSERT + UPDATE. Setiap tabel yang di-upsert butuh policy INSERT **dan** UPDATE, bukan salah satu | `RLS` `upsert` `blood_stock` `policy` `violates` | ✅ SELESAI |

### Fix #10 — Perubahan stok & sandi hilang sendiri (mode demo diam-diam aktif)
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-12 | `js/config.js:7`, `sw.js`, `app.js:1133` | Ganti sandi dan ubah stok tampak berhasil, tapi begitu browser dibuka lagi semuanya kembali ke nilai semula | `js/config.js` baris 7 kehilangan kutip penutup (`anonKey: "sb_publishable_...};`) → `Uncaught SyntaxError: Invalid or unexpected token` → `window.SUPABASE_CONFIG` tidak pernah terbentuk → `app.js:62 demo: !SUPABASE_READY` bernilai true → aplikasi masuk MODE DEMO yang menulis ke `localStorage` (`demoSet()`, `d.ownerPassword` di `app.js:974`) alih-alih ke Supabase. Tidak ada indikator visual, hanya `console.info`. Diperparah `sw.js`: handler fetch menyimpan SETIAP respons GET same-origin ke cache lalu menyajikannya cache-first, sehingga Fix #5 (mengeluarkan `config.js` dari `ASSETS_TO_CACHE`) sebenarnya belum menutup lubangnya | 1) Perbaiki kutip di `config.js` + `node --check`. 2) `sw.js`: `CACHE_NAME` → `v6` dan `js/config.js` dipaksa network-first lewat cabang khusus di handler fetch. 3) `app.js`: tampilkan banner merah permanen di layar saat mode demo aktif | `window.SUPABASE_CONFIG` mengembalikan objek ✓; Console `No Issues` ✓; banner demo hilang ✓ | Selalu `node --check` file config sebelum commit — satu kutip hilang membatalkan seluruh file. Mode fallback WAJIB terlihat di layar; fallback diam-diam membuat orang mengira datanya tersimpan | `config.js` `SyntaxError` `mode demo` `localStorage` `service worker` `cache-first` | ✅ SELESAI |

### Fix #11 — NEXT_PUBLIC_SUPABASE_URL berisi key, bukan URL
| Tanggal | File | Masalah | Akar | Fix | Verifikasi | Pelajaran | Log Keyword | Deploy |
|---------|------|---------|------|-----|------------|-----------|-------------|--------|
| 2026-08-12 | Env var Vercel, `api/freeze.js:6` | `/api/test` melaporkan `success:true` padahal `url_preview` berisi `sb_publishable_...` | Nilai env var `NEXT_PUBLIC_SUPABASE_URL` terisi publishable key. `api/test.js` hanya mengecek variabelnya ADA (`!!process.env...`), tidak memeriksa isinya, jadi lolos. Akibatnya `createClient(supabaseUrl, supabaseKey)` di `freeze.js:15` menerima URL palsu → cron pembekuan tidak pernah jalan | Perbaiki nilai env var di Vercel menjadi `https://czsnmghckhweltsedgqf.supabase.co` → Redeploy | `/api/test` → `url_preview` berupa alamat supabase.co ✓; `/api/freeze` → `{"success":true,"frozen":0,...}` ✓ | Health check yang cuma mengecek "variabel ada" itu palsu — periksa juga BENTUK nilainya (awalan `https://`, awalan `sb_secret_`) | `NEXT_PUBLIC_SUPABASE_URL` `url_preview` `freeze` `env` | ✅ SELESAI |
